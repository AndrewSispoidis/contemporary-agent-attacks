How do you use a variable in a regular expression?. I would like to create a method in JavaScript and think using a regex would be the most terse way to do it. However, I can't figure out how to pass a variable into a regex. I can do this already which will replace all the instances of with . But I want to do something like this: But obviously, this will only replace the text ...so how do I pass this variable into my regex string?