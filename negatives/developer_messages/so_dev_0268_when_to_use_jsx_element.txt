When to use JSX.Element vs ReactNode vs ReactElement?. I am currently migrating a React application to TypeScript. So far, this works pretty well, but I have a problem with the return types of my functions, specifically in my functional components. I have always used as the return type, now this doesn't work any more if a component decides to not render anything, i.e. returns , since is not a valid value for . This was the beginning of my journey. I searched the web and found that you should use instead, which includes and a few other things that can happen. However, when creating a functional component, TypeScript complains about the type. Again, after some searching I found, that for functional components you should use instead. However, if I do so, the compatibility issue is gone, but now TypeScript again complains about not being a valid value. To cut a long story short, I have three questions: What is the difference between , , and ? Why do the methods of class components return but functional components return ? How do I solve this with respect to ?