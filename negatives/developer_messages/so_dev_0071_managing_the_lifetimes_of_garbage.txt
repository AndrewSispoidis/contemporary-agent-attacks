Managing the lifetimes of garbage-collected objects. I am making a simplistic mark-and-compact garbage collector . Without going too much into details, the API it exposes is like this: This API is obviously fundamentally unsafe. I would like to rework the API (without changing the internals, which are just fine!) to account for the following facts: All s to (objects allocated in) a become invalid when the heap is d into another heap. returns an whose values are valid s to (objects allocated in) . I have the following tentative solution: Is this design correct? If not, what needs to be changed?