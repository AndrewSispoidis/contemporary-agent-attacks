XmlSerializer - There was an error reflecting type. Using C# .NET 2.0, I have a composite data class that does have the attribute on it. I am creating an class and passing that into the constructor: I am getting an exception saying: There was an error reflecting type. Inside the data class there is another composite object. Does this also need to have the attribute, or by having it on the top object, does it recursively apply it to all objects inside?