YAML current date in rmarkdown. I'm wondering if there's a trick to put the current date in the YAML front-matter of a document to be processed by and the package. I used to have the following line at the top of my wiki pages, and it would get converted to baptiste, 03 May, 2014 in the html output. Now, I would like to take advantage of the advanced pandoc wrapper provided by , but having r code in the YAML header doesn't seem to work: Any workaround?