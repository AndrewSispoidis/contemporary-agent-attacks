@class vs. #import. It is to my understanding that one should use a forward-class declaration in the event ClassA needs to include a ClassB header, and ClassB needs to include a ClassA header to avoid any circular inclusions. I also understand that an is a simple so that an include only happens once. My inquiry is this: When does one use and when does one use ? Sometimes if I use a declaration, I see a common compiler warning such as the following: Would really love to understand this, versus just removing the forward-declaration and throwing an in to silence the warnings the compiler is giving me.