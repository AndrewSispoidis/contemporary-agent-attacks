How to deal with persistent storage (e.g. databases) in Docker. How do people deal with persistent storage for your Docker containers? I am currently using this approach: build the image, e.g. for PostgreSQL, and then start the container with IMHO, that has the drawback, that I must not ever (by accident) delete container "c0dbc34fd631". Another idea would be to mount host volumes "-v" into the container, however, the userid within the container does not necessarily match the userid from the host, and then permissions might be messed up. Note: Instead of you can also use where is a name you assigned to a data-only container, e.g. (see the accepted answer)