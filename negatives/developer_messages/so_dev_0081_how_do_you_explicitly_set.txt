How do you explicitly set a new property on `window` in TypeScript?. I setup global namespaces for my objects by explicitly setting a property on . TypeScript underlines and complains that: The property 'MyNamespace' does not exist on value of type 'window' any" I can make the code work by declaring as an ambient variable and dropping the explicitness but I don't want to do that. How can I keep in there and make TypeScript happy? As a side note I find it especially funny that TypeScript complains since it tells me that is of type which by definitely can contain anything.