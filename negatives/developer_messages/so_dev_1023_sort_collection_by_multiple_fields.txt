Sort collection by multiple fields in Kotlin. Let's say I have a list of People which I need to sort by Age first and then by Name. Coming from a C#-background, I can easily achieve this in said language by using LINQ: How does one accomplish this using Kotlin? This is what I tried (it's obviously wrong since the output of the first "sortedBy" clause gets overridden by the second one, which results in a list sorted by Name only)