How to add a favicon to a Next.js static site?. I'm trying to add a favicon to a Next.js static site without much luck. I've tried customising the document with components from https://nextjs.org/docs/#custom-document A straight link to the favicon.ico file doesn't work because the file isn't included in the build and the href doesn't update to Importing the image and adding to the link's href doesn't work either (see commented out lines). The favicon links get added however it doesn't display. I'd expect it to work when I import the file, but it just adds a link. Has anyone done this yet?