GraphQL Expected Iterable, but did not find one for field xxx.yyy. I'm currently trying GraphQL with NodeJS and I don't know, why this error occurs with the following query: I am not sure if the of my is right, because at any example I had a look at they used , but in my case I really want to return a single user object, not an array of users. My code: Error: So my schema provides a field which returns the right data (the console.log is called).