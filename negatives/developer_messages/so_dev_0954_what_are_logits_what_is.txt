What are logits? What is the difference between softmax and softmax_cross_entropy_with_logits?. In the tensorflow API docs they use a keyword called . What is it? A lot of methods are written like: If is just a generic input, why is it named ? Secondly, what is the difference between the following two methods? I know what does, but not the other. An example would be really helpful.