Service located in another namespace. I have been trying to find a way to define a service in one namespace that links to a Pod running in another namespace. I know that containers in a Pod running in can access defined in by referencing it in the cluster DNS as , but I would rather not have the code inside the container need to know about the location of . That is, I want the code to just lookup and then be able to access it. The Kubernetes documentation suggests that this is possible. It says that one of the reasons that you would define a service without a selector is that You want to point your service to a service in another Namespace or on another cluster . That suggests to me that I should: Define a service in , without a selector (since the POD I want to select isn't in ). Define a service (which I also called ) in , and then Define an Endpoints object in to point to in . It is this third step that I have not been able to accomplish. First, I tried defining the Endpoints object this way: That seemed the logical approach, and obviously what the was for. But, this led to an error saying that the field in the array was mandatory. So, my next try was to assign a fixed ClusterIP address to in , and put that in the IP field (note that the is configured as , and was assigned as the ClusterIP for in ; in was auto assigned a different ClusterIP on the subnet): That was accepted, but accesses to in did not get forwarded to the Pod in - they timed out. Looking at the iptables se