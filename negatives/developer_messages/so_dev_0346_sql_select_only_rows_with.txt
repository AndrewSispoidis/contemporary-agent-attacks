SQL select only rows with max value on a column. I have this table for documents (simplified version here): id rev content 1 1 ... 2 1 ... 1 2 ... 1 3 ... How do I select one row per id and only the greatest rev? With the above data, the result should contain two rows: and . I'm using MySQL . Currently I use checks in the loop to detect and over-write old revs from the resultset. But is this the only method to achieve the result? Isn't there a SQL solution?