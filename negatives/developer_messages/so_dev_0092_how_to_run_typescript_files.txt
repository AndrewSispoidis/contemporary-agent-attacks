How to run TypeScript files from command line?. I'm having a surprisingly hard time finding an answer to this. With plain Node.JS, you can run any js file with , with CoffeeScript it's and ES6 has . How do I do the same with Typescript? My project has a which is used by Webpack/ts-loader to build a nice little bundle for the browser. I have a need for a build step run from the console before that, though, that would use some of the files used in the project to generate a schema, but I can't seem to be able to run a single Typescript file without compiling the whole project.