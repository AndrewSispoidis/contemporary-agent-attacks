What is the difference between MUST and SHOULD in an Elasticsearch bool query?. What is the difference between and in a bool query in Elasticsearch? If I ONLY want results that contain my terms, should I then use ? For example, I have a query that should only contain certain values, and should also have no results that has a date/timestamp lower than today's . Also, can I use multiple filters inside a query like the following code?