How do I turn a C# object into a JSON string in .NET?. I have classes like these: And I would like to turn a object into a JSON string like this: (Without the formatting). I found this link , but it uses a namespace that's not in .NET 4 . I also heard about JSON.NET , but their site seems to be down at the moment, and I'm not keen on using external DLL files. Are there other options besides manually creating a JSON string writer?