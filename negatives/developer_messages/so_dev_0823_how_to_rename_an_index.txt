how to rename an index in a cluster?. I need to rename several indexes in a cluster (their name must be changed, I cannot use aliases ). I saw that there are no supported ways to do that, the closest I found is to rename the directory of the index , I tried this in a cluster. The cluster has 3 machines , and and the shards are replicated on each of them. I shut down elasticsearch on , renamed to and restarted . The state of the cluster was yellow and elasticsearch was doing some magic to restore a correct state. After some time I ended up with being available and fully replicated (recovered from and I guess) being available (I can search it) but the head plugin shows that its shards are in an "Unassigned" state and that they are grayed out (not replicated) During the recovery showed the following message: While is searchable, it is certainly not in a normal state. I rolled back to the previous state by deleting . The cluster is back to green without any "Unassigned" entries. Given that, how can I rename to in a cluster? Note: the ultimate solution I have in mind is to scroll-copy into and delete afterwards. This is going to take time so if there is a more direct solution it would be great.