Converting between datetime, Timestamp and datetime64. How do I convert a object to a (or )? In the following code, I create a datetime, timestamp and datetime64 objects. Note: it's easy to get the datetime from the Timestamp: But how do we extract the or from a ( )? . Update: a somewhat nasty example in my dataset (perhaps the motivating example) seems to be: which should be , and not a long (!) ( )...