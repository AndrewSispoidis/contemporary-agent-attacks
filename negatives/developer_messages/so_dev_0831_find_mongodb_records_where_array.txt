Find MongoDB records where array field is not empty. All of my records have a field called "pictures". This field is an array of strings. I now want the newest 10 records where this array IS NOT empty. I've googled around, but strangely enough I haven't found much on this. I've read into the $where option, but I was wondering how slow that is to native functions, and if there is a better solution. And even then, that does not work: Returns nothing. Leaving without the length bit does work, but then it also returns empty records, of course.