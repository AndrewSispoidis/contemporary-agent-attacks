Check whether a string matches a regex in JS. I want to use JavaScript (I can also use jQuery) to do check whether a string matches the regex , and get a or result. seems to check whether part of a string matches a regex, not the whole thing. Does it solve the problem? Can I adapt it to solve the problem? How?